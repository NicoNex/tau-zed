# Build artifacts
target/
*.wasm

# Grammar checkouts (managed by Zed during build)
grammars/tau/
grammars/json/

# The grammar is built by Zed from the repository named in extension.toml.
# languages/ is not ignored: Zed reads the language config and the queries
# from there, and an extension without them highlights nothing.
languages/*/node_modules/
